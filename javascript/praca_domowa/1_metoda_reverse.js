//odwrocony napis

var napis = ['a', 'k', 'a', 'd', 'e', 'm', 'i', 'a', '1', '0', '8'];

var normalnyNapis = napis.join("");
console.log(normalnyNapis);

napis.reverse();
console.log(napis);

var odwroconyNapis = napis.join("");
console.log(odwroconyNapis);